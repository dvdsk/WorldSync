// use crate::gui::{style, RpcConn};
// pub use protocol::ServiceClient;
// use protocol::{tarpc, Host, Uuid};
// pub use tarpc::context;

// use super::{Error, Event, Msg, State};
// use futures::future;
// use iced::Command;

// impl State {
//     pub fn request_to_host(&self) {
//         let rpc = self.rpc.as_mut()
//             .expect("rpc connection should exist on host page");

//         rpc.request_to_host()

//     }
// }
